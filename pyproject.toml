[tool.isort]
profile = "black"
skip = ["./modules/", "./build/"]
dependencies = []

[tool.black]
line-length = 120
skip-string-normalization = true

[tool.eagle]
local-dependencies = []

[tool.mypy]
ignore_missing_imports = true
exclude = [
    'modules/',
    'build/',
    'cmake-build-debug/',
    '/setup\.py$'
]
